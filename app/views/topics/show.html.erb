<%- model_class = Topic -%>
<div class="page-header">
  <h1>
    <%= link_to "#{@event.name} >", event_path(@event), class: 'small' %><%= link_to 'Todos os tópicos > ', event_path(@event), class: 'small' %><%= @topic.title %>
  </h1>
</div>

<div>
  <%= render @topic.messages %>
</div>

<hr>

<h4>Nova mensagem</h4>

<%= render 'messages/form', message: @topic.messages.build %>


<!-- <div class="form-actions">
  <%= link_to t('.edit', :default => t("helpers.links.edit")),
              edit_event_topic_path(@topic), :class => 'btn' %>
  <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
              event_topic_path(@topic),
              :method => 'delete',
              :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
              :class => 'btn btn-danger' %>
</div>
 -->
